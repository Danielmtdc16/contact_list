import 'package:flutter/material.dart';
import '../constantes.dart';

class CardContact extends StatelessWidget {

  final String? letter;

  const CardContact({Key? key, this.letter}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(

    );
  }
}
